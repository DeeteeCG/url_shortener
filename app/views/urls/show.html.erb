<h3>This is urls/show</h3>
  <p><%= @url.long_url %></p>
  <p><%= @url.short_url %></p>
  <%= link_to 'Back', urls_path %>
  <!-- above code is same as code below... -->
  <!-- <%= link_to 'Back', controller: :urls, action: :index %> -->
